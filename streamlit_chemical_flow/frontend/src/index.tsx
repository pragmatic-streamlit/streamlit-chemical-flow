import React from "react"
import ReactDOM from "react-dom"
import ChemicalFlow from "./ChemicalFlow"

ReactDOM.render(
  <React.StrictMode>
    <ChemicalFlow />
  </React.StrictMode>,
  document.getElementById("root")
)
